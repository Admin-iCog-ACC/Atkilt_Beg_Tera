import 'package:flutter/material.dart';

class MyShops extends StatefulWidget {
  @override
  MyShopsState createState() => MyShopsState();
}

class MyShopsState extends State<MyShops> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Padding(
        padding: EdgeInsets.all(5),
      ),
    );
  }
}
